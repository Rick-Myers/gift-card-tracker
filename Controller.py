import sys

class GiftCardController(object):

	def __init__(self):
		#instantiate model
		#instantiate view
	
	def handle(self, request):
		#begin handling gift card requests

def main(main):
	request_handler = GiftCardController()
	request_handler.handle(name)

if __name__ == "__main__":
	#begin execution with some data from user